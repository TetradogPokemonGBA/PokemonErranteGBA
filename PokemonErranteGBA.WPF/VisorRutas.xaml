<UserControl x:Class="PokemonErranteGBA.WPF.VisorRutas"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PokemonErranteGBA.WPF"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
        <Button x:Name="btnAñadir" Content="Añadir" Click="btnAñadir_Click" IsEnabled="False" />
        <Button x:Name="btnCheck" Content="Check" Click="btnCheck_Click" IsEnabled="False" Grid.Column="1" />
            <Button x:Name="btnSave" Content="Guardar" Click="btnSave_Click" IsEnabled="False" Grid.Column="2" />
        </Grid>
        <ScrollViewer VerticalScrollBarVisibility="Auto" CanContentScroll="True" >
            <StackPanel x:Name="stkSaltos"/>
        </ScrollViewer>
        
    </StackPanel>
</UserControl>
